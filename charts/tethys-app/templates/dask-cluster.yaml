{{- if .Values.dask.enabled -}}
apiVersion: kubernetes.dask.org/v1
kind: DaskCluster
metadata:
  name: {{ include "tethys-app.daskName" . }}
  labels:
    {{- include "tethys-app.labels" . | nindent 4 }}
spec:
  scheduler:
    service:
      type: ClusterIP
      selector:
        dask.org/cluster-name: {{ include "tethys-app.daskName" . }}
        dask.org/component: scheduler
      ports:
        - name: tcp-comm
          port: 8786
          targetPort: tcp-comm
          protocol: TCP
        - name: http-dashboard
          port: 8787
          targetPort: http-dashboard
          protocol: TCP
    spec:
      serviceAccountName: {{ include "tethys-app.serviceAccountName" . }}
      containers:
        - name: scheduler
          image: {{ include "tethys-app.daskImage" . | quote }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args: [{{ .Values.dask.worker.schedulerCommand | quote }}]
          ports:
            - name: tcp-comm
              containerPort: 8786
            - name: http-dashboard
              containerPort: 8787
          readinessProbe:
            tcpSocket:
              port: tcp-comm
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            {{- toYaml .Values.dask.scheduler.resources | nindent 12 }}
  worker:
    replicas: 0
    spec:
      serviceAccountName: {{ include "tethys-app.serviceAccountName" . }}
      {{- with .Values.dask.worker.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: worker
          image: {{ include "tethys-app.daskImage" . | quote }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args:
            - {{ .Values.dask.worker.command | quote }}
            - {{ include "tethys-app.daskSchedulerAddress" . | quote }}
            - --name=$(DASK_WORKER_NAME)
            - --nthreads={{ .Values.dask.worker.nthreads }}
            - --memory-limit={{ .Values.dask.worker.memoryLimit }}
            - --local-directory=/scratch
          env:
            {{- range $k, $v := .Values.dask.worker.env }}
            - name: {{ $k }}
              value: {{ $v | quote }}
            {{- end }}
          volumeMounts:
            - name: scratch
              mountPath: /scratch
          resources:
            {{- toYaml .Values.dask.worker.resources | nindent 12 }}
      volumes:
        - name: scratch
          emptyDir: {}
---
apiVersion: kubernetes.dask.org/v1
kind: DaskAutoscaler
metadata:
  name: {{ include "tethys-app.daskName" . }}
  labels:
    {{- include "tethys-app.labels" . | nindent 4 }}
spec:
  cluster: {{ include "tethys-app.daskName" . }}
  minimum: {{ .Values.dask.autoscaler.minimum }}
  maximum: {{ .Values.dask.autoscaler.maximum }}
{{- end }}
